services:
  app:
    ports:
      - "5678:5678"
    volumes:
      - .:/app
    command: >
      sh -c "poetry install --with dev --no-root &&
             python -m debugpy --listen 0.0.0.0:5678 -m uvicorn main:app --host 0.0.0.0 --port 8000 --reload"

  db:
    ports:
      - "5432:5432"
